## МАШИННЫЙ ПЕРЕВОД
 1. Для перевода используется сервис MyMemory (см ключи в .env with MYMEMORY).
 2. Сделано на сегодня:
    1. В режиме редактрования записи автоматически заполняются пустые языковые поля переводом на соответствующий язык (если хотя бы на одном языке аналогиное поле заполнено)
    2. к автоматически переведенной записи добавляется метка <мп> <машинный перевод>
 3. Планы:
    1. добавить машинное обучение;
    2. при достижении высокого качества перевода - полностью.
 4. функция переводчик app/core/utils/translation_utils.py fill_missing_translations
 5. присоединяется через Annotated[Callable,Depends[get_translator_func]] в следуюшие роутеры
    1. app/support/read/router.py::def endpoint
    2. app/support/item/router_item_view.py::get_one
 6. 